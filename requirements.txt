flask>=3.0
yt-dlp>=2024.10.22
tzdata; platform_system == "Windows"   # IANA timezones for zoneinfo (Campaigns page)
# optional — only needed for the Gmail CRM sync (Phase 2). See SETUP_GMAIL.md
google-api-python-client>=2.0
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2
# optional — only needed when TURSO_DATABASE_URL is set (hosted DB). See SETUP_TURSO.md
libsql>=0.1.8
# production WSGI server — hosted deployment only (Linux). See SETUP_HOSTING.md
gunicorn>=23.0; platform_system != "Windows"
